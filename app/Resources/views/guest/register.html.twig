{# app/Resources/views/registration/register.html.twig #}
{% extends 'baseUser.html.twig' %}


{% block nav %}
    <div class="nav-bar">
        <div class="nav-logo"><a href="{{ path('employee-dashboard') }}" ><img src="{{ asset("images/logocs.png") }}" width="75" height="75" alt="logo"></a></div>
        <div class="nav-items">
            <ul>
                {#                <li><a href="#">Contact</a></li>#}
            </ul>
            <a class="btn" href="{{ path('login') }}">Login</a>
            <a class="btn" style="margin-right: 2%" href="{{ path('guestpage') }}">Home</a>
        </div>
    </div>
{% endblock %}

{% block content %}
<div class="textCreateAccount center-align white-text"><p>Create an account</p></div>
{{form_start(form) }}
<div class="row white-text">
    <div class="col s3"></div>
    <div class="col s6">
        <div class="row">
            <div class="input-field col s12 m6">
                {{ form_row(form.username) }}
            </div>
            <div class="input-field col s12 m6 ">
                {{ form_row(form.email) }}
            </div>
            <div class="input-field col s12 m6 form_row" >
                {{ form_row(form.plainPassword.first) }}
            </div>
            <div class="input-field col s12 m6">
                {{ form_row(form.plainPassword.second) }}
            </div>
            <div class="input-field col s12" >
                {{ form_row(form.submit) }}
            </div>

        </div>
    </div>
</div>

        {{ form_end(form) }}
        {% if msg is not null %}
            <div class="input-field_aiu col s12 center-align">
            {{ msg }}
            </div>
        {% endif %}
{% endblock %}

{% block stylesheets %}
    {{ parent() }}

    <style>
        body{

            font-family: 'Roboto', sans-serif;
        }
        .textCreateAccount{
            font-size: 40px;
        }
        .btn{
            background: rgba(108, 164, 57, 0.95);
            display: block;
            margin-left: auto;
            margin-right: auto;
            width:100px;
            height:40px;
        }

        .input-field_aiu{
            font-size: 25px;
        }
    </style>
{% endblock %}