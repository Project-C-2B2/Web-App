{% extends 'baseUser.html.twig' %}

{% block nav %}
    <div class="nav-bar">
        <div class="nav-logo"><a href="{{ path('employee-dashboard') }}" ><img src="{{ asset("images/logocs.png") }}" width="75" height="75" alt="logo"></a></div>
        <div class="nav-items">
            <ul>
{#                <li><a href="#">Contact</a></li>#}
            </ul>
            <a class="btn" href="{{ path('user_registration') }}">Register</a>
            <a class="btn" style="margin-right: 2%" href="{{ path('guestpage') }}">Home</a>
        </div>
    </div>
{% endblock %}

{% block content %}


    <div class="row">
        <div class="col s12">
{#            <img class="cim-img" src="https://media.glassdoor.com/sqll/419019/cimsolutions-squarelogo-1562828823772.png">#}
            <img class="cim-img" src="{{ asset("images/logocs.png") }}">
        </div>
    </div>
    <div class="row">
        <div class="col s12">
            <div class="login center-align"><p class="login-text">Sign in</p></div>
        </div>
    </div>
    {% if error %}
        <div class="col s12 center-align" >{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        <div class=" center-align"><p>The manager needs to enable the account, please wait</p></div>
    {% endif %}
    {% for message in app.flashes('notice') %}
        <div class="flash-notice center-align">
            {{ message }}
        </div>
    {% endfor %}
    <div class="row">
        <div class="col s3"></div>
        <form class="col s6" action="{{ path('login') }}" method="post">
            <div class="row">
                <div style="color: #00a7d0" class="input-field col s12 m6">
                    <img style="padding-right: 10px" class="material-icons prefix filterImg" src="https://i.pinimg.com/originals/0c/3b/3a/0c3b3adb1a7530892e55ef36d3be6cb8.png"></img>
                    <input class="white-text" type="text" id="username" name="_username" value="{{ last_username }}" />
                    <label class="white-text" for="username">Username</label>
                </div>
                <div class="input-field col s12 m6">
                    <img style="padding-right: 10px; width: 43px; height:33px"class="material-icons prefix filterImg" src="https://cdn.onlinewebfonts.com/svg/img_398183.png"></img>
                    <input class="white-text" type="password" id="password" name="_password" />
                    <label class="white-text" for="password">Password</label>
                </div>
                <div class="col s12">
                    <input type="hidden" name="_target_path" value="/login/redirect"/>
                    <button class="btn" type="submit">Sign In</button>
                </div>
            </div>

        </form>
    </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>

        .cim-img {
            display: block;
            margin-left: auto;
            margin-right: auto;
            margin-top: 5%;
            width:200px;
            height:200px;
        }
        .login{
            color: white;
            font-family: 'Roboto', sans-serif;
            font-size: 60px;
        }
        .btn{
            background: rgba(108, 164, 57, 0.95);
            display: block;
            margin-left: auto;
            margin-right: auto;
            width:100px;
            height:40px;
        }
        .filterImg{
            filter: invert(100%) sepia(0%) saturate(0%) hue-rotate(127deg) brightness(100%) contrast(101%);
        }

    </style>
{% endblock %}