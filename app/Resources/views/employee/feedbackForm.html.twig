{% extends 'baseContent.html.twig' %}

{% block content %}
    <div class="current-page">
        <h2>Feedback</h2>
    </div>
    {% for message in app.flashes('notice') %}
        <div class="flash-notice">
            {{ message }}
        </div>
    {% endfor %}
    <div class="container">
        <div class="row">
            <div class="row col-xs-12 white-text">
                <h1>Gelieve dit formulier invullen</h1>

                {{ form_start(feedbackForm) }}
                    {{ form_label(feedbackForm.rating) }}
                    {{ form_widget(feedbackForm.rating) }}
                    {{ form_row(feedbackForm.feedback, {'attr': {'class': 'white-text'}}) }}
                    <button type="submit" class="btn btn-primary" style="margin-top: 20px">Opslaan</button>
                {{ form_end(feedbackForm) }}
            </div>
        </div>
    </div>
{% endblock %}
