{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/main.css') }}">
{% endblock %}

{% block body %}
    <head>
        <meta charset="UTF-8">
        <title>Title</title>
    </head>
    <body>
    <div class="wrapper">
        {% block nav %}
            <div class="nav-bar">
                <div class="nav-logo"><a href="{{ path('employee') }}" ><img src="{{ asset("images/logocs.png") }}" width="75" height="75" alt="logo"></a></div>
                <div class="nav-items">
                    <ul>
                        <li><a href="#">Contact</a></li>
                    </ul>
                    <a class="btn" href="{{ path('login') }}">Login</a>
                </div>
            </div>
        {% endblock %}
        <div class="main-section"> {% block content %}{% endblock %} </div>
    </div>
    </body>

{% endblock %}
